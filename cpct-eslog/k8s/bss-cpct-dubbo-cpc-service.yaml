apiVersion: v1
kind: Service
metadata:
  name: bss-cpct-eslog
  labels:
    name: bss-cpct-eslog
spec:
  type: NodePort
  ports:
  - port: 30809
    nodePort: 30809
  selector:
    name: bss-cpct-eslog
