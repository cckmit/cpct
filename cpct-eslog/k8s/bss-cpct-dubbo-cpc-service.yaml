apiVersion: v1
kind: Service
metadata:
  name: bss-cpct-eslog
  labels:
    name: bss-cpct-eslog
spec:
  type: NodePort
  ports:
  - port: 30811
    nodePort: 30811
  selector:
    name: bss-cpct-eslog
